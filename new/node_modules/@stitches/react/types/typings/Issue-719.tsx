import * as Stitches from '@stitches/react'

export const Inner = Stitches.styled('div', {})

export const Outer = Stitches.styled('div', {
	[`& ${Inner}`]: {}
})

const Text = Stitches.styled('div', { color: 'GoldenRod' })

const Widget = Stitches.styled('div', {
	background: 'Pink',
	[`& ${Text}`]: {
		color: 'SeaGreen'
	}
})

export default function App() {
	return (
		<>
			<Widget>
				<Text>hello</Text>
			</Widget>
		</>
	)
}
