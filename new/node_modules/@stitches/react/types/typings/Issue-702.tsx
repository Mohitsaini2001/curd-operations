import type * as Stitches from '../index.js'
import { createStitches } from '../index.js'

// import * as Stitches from '@stitches/react'

const { css } = createStitches()

const red1: Stitches.CSS = { color: 'red' };
const text1 = css({ fontFamily: 'serif', ...red1 });
const red2: Stitches.CSS = { color: 'red' };
const text2 = css({ fontFamily: 'serif' }, red2)

void red1
void text1
void red2
void text2
