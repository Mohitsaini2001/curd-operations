import type * as Stitches from '../index.js'
import { createStitches } from '../index.js'

const { css, styled } = createStitches({})

const component = css({
	color: undefined,
	variants: {}
})

const Component = styled('div', {
	color: undefined,
	variants: {
		size: {
			1: {},
			2: {},
			3: {},
		}
	}
})

export function Page({ multiline }) {
	return (
		<Component
			size="1"
			as="p"
			css={{
				color: '$loContrast',
				lineHeight: multiline ? '20px' : undefined,
			}}
		/>
	)
}
