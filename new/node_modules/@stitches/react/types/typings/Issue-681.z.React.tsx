import type * as Stitches from '../index.js'
import { createStitches } from '../index.js'

const { styled } = createStitches({})

const Expressor = styled('div', {
	color: 'CurrentColor',
	variants: {
		color: {
			blue: {
				color: 'Blue',
			},
			pink: {
				color: 'Pink',
			},
		}
	}
})

const props: Stitches.VariantProps<typeof Expressor> = { color: 'blue' }

void props
